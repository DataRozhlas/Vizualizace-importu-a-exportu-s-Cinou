@import colors
@import prefixes
@import 'components/triangle'

.ig
  @import basics
  @import backbutton
  @import Header
.ig
  .impExpGraph
    position absolute
    top 70px
    .axis
      font-variant small-caps
      .domain
        fill black
      .tick line
        stroke black
    g.highlighted
      path
        fill-opacity 0.2
        &.highlight
          fill-opacity 1
