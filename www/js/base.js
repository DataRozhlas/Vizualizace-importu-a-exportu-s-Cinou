// Generated by LiveScript 1.2.0
(function(){
  var ig, init;
  ig = window.ig;
  ig.tsvTransform = function(row){
    var ref$, month, year, x$;
    row.cena = parseInt(row.cena, 10);
    ref$ = row.obdobi.split("/"), month = ref$[0], year = ref$[1];
    month = parseInt(month, 10);
    year = parseInt(year, 10);
    x$ = row.date = new Date();
    x$.setTime(0);
    x$.setMonth(month - 1);
    x$.setFullYear(year);
    return row;
  };
  init = function(){
    var exp, x$;
    exp = d3.tsv.parse(ig.data['export'], ig.tsvTransform);
    x$ = new ig.ImpExpGraph(d3.select(ig.containers.base));
    x$.draw(exp);
    return x$;
  };
  if (typeof d3 != 'undefined' && d3 !== null) {
    init();
  } else {
    $(window).bind('load', function(){
      if (typeof d3 != 'undefined' && d3 !== null) {
        return init();
      }
    });
  }
}).call(this);
